# ── Web Framework ─────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.9.0
python-multipart==0.0.12

# ── ML / Data ─────────────────────────────────────────────────
# Upgrading NumPy to 2.x to fix the _core error
numpy>=2.0.0
pandas>=2.2.3
scikit-learn==1.6.1
joblib==1.4.2

# ── Performance (Optional but recommended) ───────────────────
# This removes the "Pyarrow" warning you saw in the logs
pyarrow>=15.0.0